<!--
function construct_email_address(pre_at)
{
    post_at = "familiecoenen.nl";
    address = (pre_at + "&#64" + post_at);
    document.write('<a class="normal" href="mailto:' + address + '">e-mail</a>');
}

function construct_ftp_address(post_at)
{
    pre_at = "541D9F2D.cm-5-6c.";
    address = (pre_at + post_at);
    document.write('<a href="ftp://' + address + '">FTP server</a>');
}
//-->
